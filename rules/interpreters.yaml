# Interpreters: version checks for languages without dedicated rules files

allowlists:
  commands:
    - { command: "ruby --version", trust: minimal }
    - { command: "ruby -v", trust: minimal }

rules: []
